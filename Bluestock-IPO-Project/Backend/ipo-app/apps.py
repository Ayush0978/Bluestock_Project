from django.apps import AppConfig

class IpoAppConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'ipo_app'  # Replace 'ipo_app' with the actual name of your app
